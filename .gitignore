*.zip
node_modules/
__pycache__/
*.pyc
venv/
.venv/
env/
.DS_Store
Thumbs.db
*.egg-info/
.ipynb_checkpoints/
build/sections/
build/preview_*.png
build/_*.pdf

# LongMemEval bulk splits: 278 MB and 2.7 GB. Fetch with scripts/fetch_benchmarks.py --all
data/benchmarks/longmemeval/longmemeval_s.json
data/benchmarks/longmemeval/longmemeval_s_cleaned.json
data/benchmarks/longmemeval/longmemeval_m.json

# LongMemEval unpacked to one file per session: 19,206 files, about 250 MB. Rebuild with
# scripts/unpack_benchmarks.py longmemeval; the Kaggle raw dataset carries the copies.
data/raw/longmemeval/

# local benchmark scratch (LitBank clone and other rejected or oversized sets)
data/eval/

# The extractor export rebuilt locally by archive/scripts/rebuild_export.py (untracked): about 300 MB
data/export/
# The ingestor's local runs
data/packages/
data/packages-test/


# Reference PDFs for the reading list: per-paper licenses, most not redistributable. Untracked since
# 2026-09-13 (they remain in git history before that date). Fetch with archive/scripts/fetch_papers.py (untracked).
papers/*.pdf
papers/_unused/*.pdf

# LongMemEval oracle split: not used (ruling of 2026-09-04); scripts/fetch_benchmarks.py fetches it
data/benchmarks/longmemeval/longmemeval_oracle.json

# 2026-09-13: the public tree holds only what a person reads. Working tooling stays on disk, untracked:
archive/
papers/
tests/
scripts/py_to_ipynb.py
scripts/check_cell_order.py
reading/*.json
reading/paper-manifest.md
log/**/*.jsonl

# store and sidecar built locally from the Step 1 dataset
*.sqlite
*.npy
